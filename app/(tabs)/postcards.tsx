import { Text, View } from "react-native";

export default function Postcards() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Postcards</Text>
    </View>
  );
}
